<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carry.mybatis.mapper.BlogMapper">
    <select id="selectBlog" resultType="com.carry.mybatis.domain.User">
    select * from user where id = #{id}
  </select>
</mapper>